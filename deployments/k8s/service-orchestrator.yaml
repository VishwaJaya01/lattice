apiVersion: v1
kind: Service
metadata:
  name: lattice-orchestrator
  labels:
    app.kubernetes.io/name: lattice
    app.kubernetes.io/component: orchestrator
spec:
  selector:
    app.kubernetes.io/name: lattice
    app.kubernetes.io/component: orchestrator
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
    - name: metrics
      port: 2112
      targetPort: metrics
